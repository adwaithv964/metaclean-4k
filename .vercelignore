# Vercel serves only the public/ static files.
# The Node.js backend runs on Render, not Vercel.
# The vercel-build script in package.json is a no-op, so
# these server-side files are hidden for cleanliness only.
src/
dist/
tests/
scripts/
fixtures/
docs/
tmp/
Dockerfile
docker-compose.yml
render.yaml
