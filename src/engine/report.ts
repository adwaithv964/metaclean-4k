import { CleanReport } from '../types/model.js';
const esc=(s:string)=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
export function reportHtml(r:CleanReport):string { return `<!doctype html><html lang="en"><meta charset="utf-8"><title>MetaClean 4K Report</title><style>body{font-family:Inter,system-ui,sans-serif;background:#0b0d12;color:#edf1f7;margin:0;padding:32px}main{max-width:1100px;margin:auto}section{background:#121722;border:1px solid #263044;border-radius:16px;padding:20px;margin:14px 0}table{width:100%;border-collapse:collapse}td,th{padding:8px;text-align:left;border-bottom:1px solid #263044}code{font-size:12px;word-break:break-all}.ok{color:#72e0a8}.warn{color:#ffc36b}</style><main><h1>MetaClean 4K</h1><p>Generated ${esc(r.createdAt)}</p><section><h2>File information</h2><table>${[['Format',r.before.file.format],['Dimensions',`${r.before.file.width} × ${r.before.file.height}`],['Original size',String(r.before.file.size)],['Cleaned size',String(r.after.file.size)],['Original SHA-256',r.integrity.originalHash],['Cleaned SHA-256',r.integrity.cleanedHash]].map(([a,b])=>`<tr><th>${esc(a)}</th><td><code>${esc(b)}</code></td></tr>`).join('')}</table></section><section><h2>Cleaning actions</h2><p class="ok">Removed: ${esc(r.actions.removed.join(', ')||'None')}</p><p>Preserved: ${esc(r.actions.preserved.join(', ')||'None')}</p><p class="warn">Skipped: ${esc(r.actions.skipped.join(', ')||'None')}</p></section><section><h2>Integrity</h2><p>Dimensions: <b>${r.integrity.dimensions?'UNCHANGED':'CHANGED'}</b></p><p>Pixel data: <b>${esc(r.integrity.pixelData)}</b></p><p>Color profile: <b>${r.integrity.colorProfile?'UNCHANGED':'CHANGED/UNKNOWN'}</b></p><p>Alpha: <b>${r.integrity.alpha?'UNCHANGED':'CHANGED'}</b></p><p>Bit depth: <b>${r.integrity.bitDepth?'UNCHANGED':'CHANGED/UNKNOWN'}</b></p></section><section><h2>Warnings</h2><ul>${r.warnings.map(w=>`<li>${esc(w)}</li>`).join('')}</ul></section></main></html>`; }
